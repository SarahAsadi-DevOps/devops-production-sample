replicaCount: 1

image:
  repository: sarahasadi/devops-fastapi
  tag: "0.1.2"
  pullPolicy: IfNotPresent

containerPort: 8000

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

config:
  APP_NAME: "fastapi-devops-sample"
  APP_ENV: "development"
  LOG_LEVEL: "debug"

ingress:
  enabled: true
  host: fastapi-dev.local

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
