apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fastapi.fullname" . }}-config
  labels:
    app: {{ include "fastapi.name" . }}
data:
  APP_NAME: {{ .Values.config.APP_NAME | quote }}
  APP_ENV: {{ .Values.config.APP_ENV | quote }}
  LOG_LEVEL: {{ .Values.config.LOG_LEVEL | quote }}
